import CrossIcon_SVG from "../components/icons/cross_icon.svg";
import QueenIcon_SVG from "../components/icons/queen_icon.svg";

export const Examples = () => {
  return (
    <div className="text-[14px]">
      {[{ picture: "", caption: <div> </div> }].map((example, index) => (
        <div> </div>
      ))}
    </div>
  );
};
